import 'package:flutter/material.dart';

class TrackedExerciseListItemBody extends StatelessWidget {
  const TrackedExerciseListItemBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Placeholder(
      fallbackHeight: 100.0,
      color: Colors.grey,
    );
  }
}
